{
  "rules": [
    {
      "trigger": "\\b(db|database|drizzle|sql\\w*|postgres\\w*|sqlite\\w*|mysql\\w*)\\b",
      "guardrail": "## 🗄️ DATABASE GUARDRAILS\n- Always batch multiple database statements.\n- DO NOT write destructive migrations (`DROP TABLE`) without explicit user consent."
    },
    {
      "trigger": "\\b(css|theme|tailwind)\\b",
      "guardrail": "## 🎨 CSS & DESIGN GUARDRAILS\n- Use semantic design tokens from the theme rather than hardcoding hex values."
    },
    {
      "trigger": "\\b(regex|redact|secret|token|auth)\\b",
      "guardrail": "## 🔒 SECRET REDACTION GUARDRAILS\n- Never weaken token regular expressions.\n- Always test regexes against full strings without accidental literal characters inside character classes."
    },
    {
      "trigger": "\\b(glob|path|win32|windows|slash)\\b",
      "guardrail": "## 🌐 CROSS-PLATFORM PATH GUARDRAILS\n- Always replace backslashes `\\\\` with `/` before performing glob pattern matching or string splits."
    }
  ]
}
