# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
name = "guess_host_triple"
version = "0.1.5"
authors = ["Tim Allen <screwtape@froup.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Guess which Rust-supported platform is running the current code"
readme = "README.md"
keywords = [
    "host",
    "target",
    "triple",
    "install",
]
categories = ["os"]
license = "MIT/Apache-2.0"
repository = "https://gitlab.com/Screwtapello/guess_host_triple/"

[lib]
name = "guess_host_triple"
path = "src/lib.rs"

[[example]]
name = "guess-host-triple"
path = "examples/guess-host-triple.rs"

[[test]]
name = "version-numbers"
path = "tests/version-numbers.rs"

[dependencies.errno]
version = "0.2.4"

[dependencies.log]
version = "0.4.1"

[dev-dependencies.env_logger]
version = "0.5.6"

[dev-dependencies.version-sync]
version = "0.9.3"

[target."cfg(unix)".dependencies.libc]
version = "0.2.0"

[target."cfg(windows)".dependencies.winapi]
version = "0.3"
features = [
    "sysinfoapi",
    "winnt",
]
