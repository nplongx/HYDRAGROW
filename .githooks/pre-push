#!/usr/bin/env bash
set -euo pipefail

if command -v pre-commit >/dev/null 2>&1; then
  echo "Running pre-commit before push..."
  pre-commit run --all-files
else
  echo "pre-commit is not installed. Install it first: pipx install pre-commit"
  exit 1
fi
