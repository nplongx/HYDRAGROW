# ESP32-C3-SENSOR-NODE

Sensor firmware written in C++ using the Arduino framework via PlatformIO.

## Setup
1. Copy `src/secrets.h.example` to `src/secrets.h` and configure your credentials.

## Build Instructions
```bash
pio run
pio run -t upload
```
