{
  "requirement_id": "477",
  "objective": "Resolve unsafe environment variable manipulation in cloudinary tests",
  "acceptance": [
    {
      "id": "AC-1",
      "criterion": "Tests `from_env_returns_none_when_unset` and `from_env_returns_some_when_all_three_set` run serially to avoid data races.",
      "verification": "cargo test --manifest-path hydragrow-backend/Cargo.toml -- services::cloudinary",
      "evidence_required": true
    }
  ]
}
