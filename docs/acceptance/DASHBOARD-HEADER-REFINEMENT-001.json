{
  "requirement_id": "DASHBOARD-HEADER-REFINEMENT-001",
  "objective": "Rename SensorBentoCard theme 'blue' to 'sky' to match design tokens, and add AlertBell header component with unread alert badge to Dashboard.",
  "acceptance": [
    {
      "id": "AC-1",
      "criterion": "SensorBentoCard accepts theme 'sky' and renders sky theme classes text-sky-700 bg-sky-50 border-sky-100.",
      "verification": "cd hydragrow-frontend && npm run test -- src/components/ui/SensorBentoCard.test.tsx"
    },
    {
      "id": "AC-2",
      "criterion": "AlertBell component renders bell icon and displays unread count badge (or 9+ for > 9) when unreadCount > 0, calling onClick when clicked.",
      "verification": "cd hydragrow-frontend && npm run test -- src/components/ui/AlertBell.test.tsx"
    },
    {
      "id": "AC-3",
      "criterion": "Dashboard uses SensorBentoCard with theme 'sky' and renders AlertBell in header row with unread count derived from recent 24h warning/critical/error system events.",
      "verification": "cd hydragrow-frontend && npm run test -- src/pages/Dashboard.test.tsx"
    },
    {
      "id": "AC-4",
      "criterion": "Frontend code passes TypeScript compilation, ESLint, unit tests, and Vite production build.",
      "verification": "cd hydragrow-frontend && npx tsc --noEmit && npx eslint . && npm run test && npm run build"
    }
  ]
}
