{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "requirement_id": "HEALTH-LOOP-ROUTING-001",
  "objective": "Extract pure topic-resolution logic into hydragrow-controller-core and eliminate outer loop continue statements in health.rs to avoid skipping dosing/sensor/health-publish logic when topic overrides are processed.",
  "acceptance": [
    {
      "id": "AC-1",
      "criterion": "resolve_override_publish_target pure resolver extracts topic override and payload JSON or falls back to whole value, and returns None when no override is present",
      "verification": "cargo test --manifest-path hydragrow-controller-core/Cargo.toml publish_routing::"
    },
    {
      "id": "AC-2",
      "criterion": "ESP32-C3-CONTROLLER-NODE health loop handles topic override without continue statements, allowing fallthrough to subsequent channel checks and health publishing",
      "verification": "Inspection of health.rs fsm_rx and dosing_report_rx blocks showing absence of continue and correct delegation to resolve_override_publish_target"
    },
    {
      "id": "AC-3",
      "criterion": "All affected Rust code passes cargo fmt --check, cargo clippy --all-targets -- -D warnings, and unit tests",
      "verification": "cargo fmt --check and cargo clippy across workspace"
    }
  ]
}
