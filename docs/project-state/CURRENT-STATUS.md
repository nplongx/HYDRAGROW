# Current Status

- P0.2 Canonical Command Lifecycle: IN PROGRESS — shared command identity/lifecycle contract added; backend control commands now create `command_id`, persist `REQUESTED`/`SENT`, expose device-scoped lifecycle history, and publish correlated MQTT metadata; controller propagates correlation and emits `ACKNOWLEDGED`/`REJECTED`; backend reconciles `CONFIRMED` only from correlated runtime controller status; WebSocket and frontend control UI consume canonical lifecycle states. Remaining P0.2 work: explicit timeout/unknown reconciliation policy and deeper reconnect/reload coverage. Shared/backend checks, clippy, fmt, and `git diff --check` pass. Full backend test suite reaches DB-backed tests but local database-dependent tests fail without the required database; ESP32 target verification is blocked by host target selection (`x86_64-unknown-linux-gnu`) in the current environment; frontend verification remains blocked because `node`, `npm`, and `npx` are unavailable.
- P0.3 Authoritative State / Telemetry Model: IMPLEMENTED IN WORKTREE — added shared authoritative telemetry quality/availability/axis envelope with distinct `observed_at`/`received_at`, per-axis error/unknown semantics, out-of-order and partial merge protection; backend sensor ingestion now validates `IncomingSensorPayload`, stores canonical state under existing `device_states`, keeps controller health/actuator/FSM separate, and exposes only authoritative current state from `/sensors/latest`; controller contact is tracked separately through `device_topic_last_seen` and WS `last_seen_at`; frontend `useDeviceSync` consumes device-scoped `telemetry_snapshot`, removed the 65-second freshness heuristic, and Dashboard renders `UNKNOWN`/`ERROR`/`STALE` explicitly without numeric defaults. Verification: shared all 60 unit tests + integration suites (3/3) pass, authoritative tests 5/5 pass, backend sensor tests 4/4 pass, backend `cargo check` pass, `git diff --check` pass. Frontend verification remains blocked because `node`, `npm`, and `npx` are unavailable; full backend integration suite remains unverified.
- P0.4 Configuration Consistency: IMPLEMENTED IN WORKTREE — unified config reads now distinguish missing rows from DB/query errors and no longer synthesize config on query failure; required `device_config` absence returns explicit not-found; controller sync propagates sensor-config read failures; unified five-domain writes now commit atomically in one PostgreSQL transaction after dosing validation; route `device_id` is authoritative for water/sensor writes; single-domain sync failures and unified MQTT failures return `202 partial_success` instead of false `success`; frontend `useDeviceSync`/Settings no longer silently retain a valid-looking unified config after a failed read and recognizes `partial_success`. Verification: backend `cargo check` pass, backend full unit suite 359/359 pass, clippy `-D warnings` pass, `git diff --check` pass. Frontend runtime verification remains blocked because `node`, `npm`, and `npx` are unavailable. Contract: `docs/superpowers/specs/2026-09-15-p0-4-config-consistency.md`.
- P0.1 Canonical Station/Device Context: IMPLEMENTED IN WORKTREE — added `StationProvider`/`useStationContext` as the application-level selected-device identity boundary, validated persisted selection against `/devices`, rejected invalid selection without destroying a valid selection, preserved established selection on refresh failure, guarded against stale refresh results, migrated the main device-scoped pages and `useDeviceSync` to canonical identity, and removed the web first-device auto-selection path. `useDeviceStore.deviceId` remains a transitional mirror. Frontend verification is currently blocked because `node`, `npm`, and `npx` are unavailable; `git diff --check` passes. Remaining direct store identity consumers are limited to compatibility/legacy surfaces (`DevicePairing`, `ConfigBackup`, season/recipe helper hooks, and the context mirror).
- User Discovery & Strategic Research (DISC-2026-09-12-001): COMPLETED — comprehensive user discovery document delivered in `docs/discovery/2026-09-12-user-discovery.md`. Evaluated the 3 foundational open questions from `docs/hydragrow-hifi-spec.md` (single vs fleet, Operator vs Viewer permission boundaries, FCM/APNs readiness) against backend/frontend schemas and source code. Formulated SQL telemetry queries for user-device topology; framed 3 JTBD tracks across functional/emotional/social dimensions; created 2 unverified Persona hypotheses (Minh - Balcony Hobbyist, Anh Thắng - Commercial Farm Manager); mapped a 6-stage crop cycle journey and audited 5-tab IA mismatches; authored a 45-minute non-leading interview guide for 5 archetypes; and defined a quantitative InfluxDB time-series model (Z-score, EWMA, consumption baselines) to replace hardcoded alert thresholds.
- Web Firebase Auto-Auth — Remove Manual API Key Entry (FIREBASE-AUTO-AUTH-001): IMPLEMENTED — a signed-in Firebase user no longer needs to type a backend API key. Frontend: `getDefaultBackendUrl()` returns `window.location.origin` (`https://hydragrow.onrender.com` fallback), `loadWebSettings` returns a populated default `{ backend_url, api_key: '', device_id: '', grafana_url: '' }`, `hasRequiredRemoteConfig` depends only on `backend_url`; `useDeviceSync` auto-selects the first owned device via `GET /api/devices` and always fetches unified config with the Firebase Bearer token (X-API-Key gate removed); OTA/WiFi/FCM guards reduced to `backend_url`; MainLayout gate reworded to "Chưa kết nối máy chủ"; ConnectivitySection API Key marked "không bắt buộc với Firebase"; `vite.config.ts` adds a same-origin `/api` proxy (http+ws → localhost:8080). Backend: ws_handler accepts a Firebase ID token via `?token=` or `{type:'auth', token}` with signature + active-user + device-ownership check (`ws_token_authorized`); legacy `api_key` unchanged. Frontend tsc clean, eslint 0 errors (183 pre-existing warnings), vitest 88 files / 440 tests, build green. Backend cargo test 313/313 (ws:: 6/6), clippy `-D warnings` + fmt clean. Acceptance + evidence contracts for FIREBASE-AUTO-AUTH-001 (AC-1..AC-7) validated.
- Figma Design System + W1-W3 (UI-FIGMA-DS-001): IN PROGRESS (first PR) — Figma file "HydraGrow – Automation Redesign" (fileKey UvfamFTHSrneof4eKOxozm) aligned on the first three screens. Design System layer part 1 done: App.css @theme tokens (Inter --font-sans, warn-deep #78350f, soft status backgrounds success/danger/warning/info + info-fg, .app-slider thumb styles) + shared primitives src/components/ui/{Button,Badge,Slider,HealthScore,DeviceStatePill,Banner,AppToaster} with 6 unit-test files (Input/Select/Modal written + green but deferred to the W4-W8/automation follow-ups), App.tsx on <AppToaster/>. Refactors: AutomationConflictBanner/DosingAnomalyBanner/ControlPanel offline+fault banners -> ui/Banner, ControlPanel Khôi phục -> Button secondary. W1 Dashboard done: DeviceStatePill station status, next-action Banner+Badge+Button, HealthScore trạm, tank alert Banners, sparklines on 4 metric cards (SensorBentoCard sparkline prop). W2 Điều khiển done: AdvancedDeviceControl inline ui/Slider PWM '72% ≈ 8.6 ml/phút' on running card (commit via setPwm+savePwmPreference), technical/emergency raw range inputs -> ui/Slider, lock row + StatusPill kept. W3 Mùa vụ done: ActiveSeasonCard delay warning -> Banner, Sửa/Kết thúc mùa vụ -> Button, progress + photo journal kept. tsc clean, eslint 0 errors (183 pre-existing warnings), vitest 85 files / 424 tests (committed set; untracked deferred Input/Select/Modal tests take the worktree to 88/437), build green. Deferred to follow-up PRs: W4-W8 screens and the automation flow-editor retheme (ConfigPanelUI/AutomationMetricsBanner still old styling). Acceptance + evidence contracts for UI-FIGMA-DS-001 (AC-1..AC-7) validated.
- Cron Scheduler & Config Override Fix (AUTOMATION-CRON-001): VERIFIED — cron_next_run_at bootstrapped at write time and advanced strictly on every tick (base = max(now, prev) in the trigger timezone; cron cleared for non-cron triggers, script disabled on parse error), fire-time success/failure logging for cron scripts, write-time cron validation (invalid expression → 400 on create/update/validate), sensor-path chain filtering that drops cron/webhook/fsm islands from the sensor tick (only sensor-effective roots + reachable descendants run), Config·Overwrite apply/restore kept on the cron path via reconcile_cron_config_overwrite, and the legacy `config_override` kind retired (frontend enum trimmed + normalizeLegacyIr, FlowEditorHeader option removed, backend validate_kind → 400, DB migration 20260911000000 restores the 3-kind CHECK with a defensive UPDATE). Backend: cargo build/clippy `-D warnings`/fmt clean, tests 309/309 (the previously-broken multi_device_template reapply DB test was repaired as a drive-by — its device_config seed INSERT omitted required NOT NULL columns), cron_scheduler 8/8, script_eval 26/26. Frontend: tsc clean, eslint 0 errors (2 pre-existing apiClient.ts no-empty blocks fixed as drive-by), vitest 390/390, npm run build green. Acceptance + evidence contracts (AC-1..AC-6 ALL PASS) validated.
- Automation UI Overhaul: VERIFIED
- Automation Execution Context: VERIFIED
- Automation Config Override & Mockup Redesign: VERIFIED
- Automation Live Data & Empty State Integration (AUTOMATION-REDESIGN-001): VERIFIED
- Automation Flow Editor Centered Layout & Node Inspector Redesign: VERIFIED
- Automation Node Configuration Inspector Redesign (11 Node Types, AUTOMATION-REDESIGN-002): VERIFIED
- Controller FSM Remediation & Safety Invariants Hardening (CONTROLLER-FSM-002): VERIFIED
- Fleet OTA + WiFi Provisioning Transaction (OTA-WIFI-TRANSACTION-001): IMPLEMENTED — host suites green (shared 66, core 202, backend 226, frontend 262); ESP32 target build + pilot rollout pending (Tasks 13-14)
- Hi-Fi D1–D3 (HIFI-D1-D3-001): VERIFIED — pH interlock, global emergency stop, schedule conflict banner, and real-time status pills implemented and tested across backend, firmware, and frontend.
- Hi-Fi D4–D6 (HIFI-D4-D6-001): VERIFIED — crop season progress bar & delay calculation, Cloudinary photo journal, saving active crop season as recipe template, and dosing history time-range tabs with hourly chart & anomaly detection implemented and tested.
- Figma Feature-Gap Build (plan docs/superpowers/plans/2026-09-10-figma-unimplemented-features.md): IN PROGRESS — Phase A done (OrphanedPagesRoutes test + `/fleet` `/config-backup` `/user-management` routes + Settings GeneralSection rows). Phase B done (B1 Dashboard greeting/critical banner/Tưới ngay; B2 SystemLog date grouping; B3 history `N ngày`; B4 E-stop dialog "SẼ DỪNG NGAY toàn hệ thống" + PWM%; B5 FleetView shell + warning pills; B6 cron→Vietnamese schedule; B7 auth register/forgot/Google + red borders). Backend auto-provision of users from Firebase token (default `read:telemetry`, no scope wipe) with test_users.rs 6/6. Phase C: C1 acknowledge/resolve events (PUT /events/{id}/acknowledge + resolved_at migration + EventLogCard badge + "Chưa xử lý" pill), C2 log categories device/automation (shared log.rs variants + script_eval mapping + migration + SystemLog pills), C3 delete season + delete photo (DELETE routes + history row trash + journal hover trash without Cloudinary destroy) all BUILDS/TESTS GREEN (frontend tsc/build/vitest seasons 3 files 7 tests; backend build/clippy `-D warnings` clean, tests 295/296 with the single pre-existing `multi_device_template::apply_template_reapply_respects_the_current_requests_preserve_choice` FK failure reproduced unchanged at base HEAD). C5 fleet summary done: backend `api/fleet.rs` `GET /api/fleet/summary` (user-scoped, per device `{device_id, label, is_online, last_seen, crop, ec_latest, ph_latest, warning_count(1h)}` assembled from device_states cache + Influx latest + 1h system_event warnings + active stage name; 2 serialization tests, clippy/fmt clean) + FleetView now renders card EC/pH chips + ⚠n badge + 🌱 crop + "EC — · pH —" fallback from a single `/fleet/summary` call (replacing the per-device `/recipe/status` + `/health-summary` fan-out); tsc/lint/build green, route tests 5/5. C4 recipe light-hours done: shared `CropStage.light_hours: Option<i32>` (serde default) + migration `20260910100002_add_light_hours_to_crop_recipe_stages.sql` + backend recipe create/update INSERT + row SELECTs (`#[sqlx(default)]` for stale-shadow test DBs) + RecipeBuilder "Ánh sáng (giờ/ngày)" field + growth-phase preset pills (Ươm mầm 14h / Sinh trưởng 16h / Ra hoa 12h / Thu hoạch 12h) prefill name + duration_days + light_hours; shared tests 17+3, controller-core tests green (CropStage literals + light_hours: None across 6 test/util files), backend tests 295/296 (same pre-existing baseline failure), clippy `-D warnings` + fmt clean, frontend tsc/build/lint 0 errors, seasons/crops vitest 7/7. C6 scripts last-run done: migration `20260910100003_add_last_run_at_to_scripts.sql` (`user_scripts.last_run_at TIMESTAMPTZ`), `db::postgres::touch_script_last_run` set on every fired script in the sensor-tick chain (sensors.rs) and cron path (cron_scheduler.rs), `UserScript` row + `GET /devices/{id}/scripts` now serialize `last_run_at`, `FlowOverviewCard` footer shows "lần cuối: hôm nay / <thứ> / dd/MM" relative label with "Chưa từng chạy" fallback; backend sqlx test verifies touch persists + list returns it, clippy/fmt clean; frontend tsc/build green, vitest 364/364 incl. 5 new FlowOverviewCard last-run/cron tests. Remaining: C7 users role + admin API, C8 analytics health, Phase D (D1 Roles, D2 pairing QR + D15 confirm, D3 Grafana gate + weekly report).
- AI Supervisor Shared Query Crate (SUPERVISOR-QUERY-001): VERIFIED — hydragrow-supervisor-query library crate implemented with closed 6-query allowlist, validate_and_clamp bounds, device-scope checking, GET-only HttpQueryBackend, output truncation, and FakeQueryBackend for testing (14 unit tests green).
- AI Supervisor Diagnostic Worker (DIAGNOSTIC-WORKER-001): VERIFIED — hydragrow-diagnostic-worker binary crate implemented with pure trigger evaluator (Hestia state & watchdog breach), BackendClient with DiagnosisBackend trait, provider-agnostic DiagnosticModel & Anthropic tool-calling loop, per-device orchestration with dedup pre-checks and fallbacks, and bounded fleet tick (27 unit tests green).
- Notification Channel Completion & Dead Node Cleanup (AUTOMATION-009): VERIFIED — threaded notifyFcm through compile/eval/models, extracted shared handle_fired_alert for sensor and cron triggers, replaced fake Email/Webhook pills in NodeEditorPanel with real FCM override dropdown, and removed unsupported Delay node from NodePalette.
- Hi-Fi Full App Reskin (AUTOMATION-REDESIGN-003): IN PROGRESS — Phase A (token layer + shared primitives + navigation) done: Figma token set in App.css @theme, shared classes/primitive components re-styled to Hi-Fi D1-D17 palette (3-tier green hierarchy), Figma-style main header + dot/label bottom pill nav, CHUAN-GIAO-DIEN-FRONTEND.md updated (indigo automation-only exception). AC-1..AC-5 PASS (acceptance + evidence contracts validated). Phase B (D1-D3 sweeps) done: Dashboard hero/tiles/pills, SensorBentoCard, QuickActionBar, ControlCard + DosingSummaryCard, ControlPanel overlay/header/fault chip, AdvancedDeviceControl pump cards (PH-fuchsia switch, toggleoff default), Operations pill tabs, Automation page shell (header, metrics banner, conflict banner #FFFBEB/#92400E, flow overview cards, filters/empty states, drawer overlay #0F1F14/40), FlowDetailDrawer chrome → slate/indigo editor theme. AC-6..AC-8 PASS - acceptance + evidence contracts validated. Phase C (D4-D8 sweeps) done: CloseSeasons/DosingHistory/RecipeBuilder + SystemLog/Analytics + logs components (EventLogCard, CycleEventCard, EventDetailDrawer, HealthSummaryBar, MetadataRenderers) tokenized; App.css shadows/focus-rings tokenized; water/dosing/sensor identity tints (sky/cyan/orange/purple/red) kept. AC-9..AC-10 PASS - acceptance + evidence contracts validated (10/14 criteria verified). Phase D (D9-D17 sweeps) done: Settings page/GeneralSection/ThresholdsSection/ConnectivitySection/DangerZoneSection + FleetView + DevicePairing + UserManagement + EmergencyStopConfirmDialog + FsmStatusBadge tokenized; A/B/C straggler shadows (shadow-emerald-950/5|10 -> shadow-primary) + slider tracks (emerald-100 -> bg-line) + InputGroup/AccordionSection/SubCard/MainLayout logo cleaned; banned-class repo audit (emerald-950/emerald-200/bg-blue-*/bg-indigo-*/bg-slate-* outside automation) clean. AC-11..AC-13 PASS - acceptance + evidence contracts validated (13/14 criteria verified). Phase E close-out done: AC-14 PASS - tsc clean, eslint 0 errors (170 pre-existing warnings), vitest full 343/343 (SystemLog pagination timeout flake passes isolated), build green, banned-class repo audits clean (emerald-950/emerald-200/bg-blue-/bg-indigo-/bg-slate- outside automation = 0 hits). ALL 14 acceptance criteria (AC-1..AC-14) PASS - acceptance + evidence contracts validated. AUTOMATION-REDESIGN-003 COMPLETE.
- Diagnostic Hestia + Local LLM (DIAGNOSTIC-HESTIA-LOCAL-001): VERIFIED — fleet Hestia endpoint (GET /api/health/hestia, {status,data} envelope, 7 backend tests green), worker envelope fix + trigger/tool-safety locks + local Qwen3/llama.cpp round-trip and budget tests + JSON extraction & tool argument resilience (worker 45/45 green); touched files rustfmt-clean, clippy clean; verified live against Android/Termux environment (SSH_OK, llama-server running, /api/health/hestia HTTP 200).
- Recipe & Alert Authorization Scopes (SEC-RECIPE-ALERT-SCOPE-001): IMPLEMENTED (branch fix/sec-recipe-alert-scope-001) — declared `events:write` + `recipe:write` in `KNOWN_SCOPES` with descriptions (wired the previously orphaned `api::tests` module + undeclared `scope_definitions` module so scope tests actually compile/run); `resolve_event` now requires `events:write` (was `read:telemetry`); recipe CRUD requires `recipe:write` (scope checked before validation, script.rs pattern); `apply_recipe`/`bulk_apply_recipe` require scope AND ownership with API-key-without-user rejected (bulk keeps 401 for missing user); `clear_recipe` now enforces scope + ownership on the path `device_id` (previously unchecked). No migration touched; frontend unchanged (Roles CAPABILITIES already match; RecipeBuilder toasts surface 403 status text). Backend build/clippy `-D warnings`/fmt clean; 32 new auth tests green (5 scope, 8 alert, 19 recipe); full suite 274 passed with 70 pre-existing `DATABASE_URL must be set` environmental failures (no local DB; CI provides postgres service). Acceptance + evidence contracts (AC-1..AC-6 ALL PASS) validated.
- Roles Capability Matrix Alignment (ROLES-CAPABILITY-ALIGNMENT-001): IMPLEMENTED — split the conflated Roles `admin` capability into separate OTA & Device Network (`device:ota`, `device:network`; Operator allowed) and Permissions & Member Invitation (Admin-only) rows; `ROLE_DEFAULT_SCOPES` unchanged. Added `Roles.capabilities.test.ts` to compare every matrix flag against `ROLE_DEFAULT_SCOPES` at test time. Acceptance + evidence contracts (AC-1..AC-3 ALL PASS) validated.
- Design System Consistency & Governance Audit (DESIGN-SYSTEM-AUDIT-001): IMPLEMENTED (branch chore/design-system-consistency-audit, 8 commits 789b89e4..HEAD) — color-drift guard + Roles status pill via DeviceStatePill, 5 orphan ui-*/farm-* classes eliminated (Switch reuse + .ui-state-icon), --color-faint darkened #7c9385→#556d5e for WCAG AA 4.5:1, EmergencyStopButton touch-target/color-uniqueness guard, Settings tabs 5→4 (sensor merged into growth), 3 design-system docs (PATTERN-LIBRARY, PR-QA-CHECKLIST, GOVERNANCE). Vitest guards green; project-state traceability updated.
- Layer 2 Design/UX Rollout (LAYER2-DESIGN-UX-ROLLOUT-001): IMPLEMENTED — full 6-track implementation across 26 tasks. Track A (state-machine & error-handling-ux: pumpControlStateMachine, PumpControlStatePill, pumpVisualTheme, AdvancedDeviceControl banner/state-machine refactor, StatusPill tokenization, ControlCard removal); Track B (data-visualization: hourlyBucketsByPump, DosingHourlyChart 24h grouped-bar with accessible sr-only table, DosingTotalCard & DosingHistory refactored, DosingReportCard tokens, Sparkline & useHealthHistory ring-buffer on Analytics); Track C (form-design: validateDeviceId on-blur validation with aria-invalid/describedby, ScanConfirmOverlay inline error & onRetryScan rescan recovery, DevicePairing tokens); Track D (search-ux: HealthSummaryBar clear search & count, zero-results state with query echo & escape, splitByMatch safe highlight, EVENT_CATEGORY_THEME tokens across logs); Track E (zeigarnik & peak-end: remainingDays & stageChecklist in seasonProgress, SeasonStageChecklist, SeasonCompletionSummary dialog, ActiveRecipeStatus tokens); Track F (governance & docs sync: PATTERN-LIBRARY.md sections 5-10, DESIGN.md §15 tracked debt, CHUAN-GIAO-DIEN-FRONTEND.md §7 components list). Full suite verified: 101/101 test files passed (496 tests), tsc --noEmit exit 0, eslint 0 errors, hardcodedColors.test.ts 0 unauthorized violations outside allowlist.
- Layer 3 Design/UX Rollout (LAYER3-DESIGN-UX-ROLLOUT-001): IMPLEMENTED — completed 5-track defensive rollout addressing Open Question 1 (household vs commercial priority). Track A (LAYER3-ROLES-COMPONENT-001): extracted PermissionMatrix, RoleBadge, and RoleSelector with ARIA grid semantics, arrow-key navigation, responsive stacked cards, and dynamic role/capability arrays. Track B (LAYER3-ROLES-FORM-002): chunked InviteForm with inline validation (UID & Email), radio role selector with scope previews, summary preview panel, and error recovery banner. Track C (LAYER3-FLEET-UX-003): redesigned FleetView with FleetStationCard, warning-first sorting, Hick's Law crop grouping (>= 4 stations), and responsive 1/2/3 column layout. Track D (LAYER3-ONBOARDING-004): progressive 4-step OnboardingWizard with localStorage persistence, reactive telemetry completion hook, empty state handling, and Peak-End celebration. Track E (LAYER3-DOCS-005): authored Q1-DECISION-FRAMEWORK.md detailing the tiered-product architecture hypothesis, evidence inventory from §11.4 competitive analysis, decision matrix, minimum viable unblocking signal, extension points catalog, and risk assessment; updated PATTERN-LIBRARY.md (sections 11-15), GOVERNANCE.md (section d), and TRACEABILITY.md. All tests passing (105 test files, 542 tests), tsc --noEmit clean with 0 errors.
- System Logging Remediation across Firmware, Backend, Watchdog, Frontend & DB (SYSTEM-LOGGING-REMEDIATION-001): IMPLEMENTED — full 17-task remediation across Proposals C1–C20. Firmware: ESP32 sensor node connects with MQTT Last Will and Testament (LWT) on TOPIC_STATUS (willPayload offline/disconnected, retain=true, qos=1); controller-core emits PublishFsmTransition on all fault/lockout transitions and adds TransitionReason::HardTimeout; pre-fault early warning logs emitted on streaks 1 & 2. Backend: mapped operational FSM transitions, OTA lifecycle status updates, and user config updates (unified, device, safety) to NewSystemEventRecord into system_events; err_* sensor flags propagated to SensorSnapshot; automation alerts enriched with script metadata. Shared & Watchdog & AI: dedicated DosingEvent and SensorEvent metadata structs in log.rs; watchdog extended with check_topic_staleness to monitor sensor/status staleness alongside controller; diagnostic worker triggers expanded for SensorStaleness and DosingAnomaly. Database & Retention: schema migration 20260913090000_enrich_dosing_and_flow_logs.sql adding ec_before, ec_after, ph_before, ph_after, cycle_id, and triggered_by to dosing_action_log, and trigger_source and duration_ms to flow_execution_log; retention cleanup service pruned logs older than 90 days. Frontend: SystemLog sensor empty state and filter guidance added. All subsystems verified: backend cargo test (356 passed), controller-core cargo test (72 passed), watchdog cargo test (17 passed), shared cargo test (47 passed), diagnostic-worker cargo test (49 passed), frontend vitest (108 files, 541 passed), tsc clean, eslint 0 errors, build green. Acceptance & evidence contracts validated.
- Frontend Device Pairing & Design Consistency (FRONTEND-DEVICE-PAIRING-UX-001): IMPLEMENTED — hardcoded backend URL to https://hydragrow.onrender.com (getDefaultBackendUrl returns Render URL unconditionally; settings test updated); pairing flow reachable via client-side routing only (Settings navigate('/pairing'), OnboardingWizard step 4 to /cultivation, legacy /seasons redirect); Dashboard and ControlPanel render without sensor data (guards removed, optional-chained access, ControlPanel no-data info banner, onboarding exposed for new users); page structure standardized (FleetView, UserManagement, ConfigBackup on app-page + page-header pattern); design-token compliance (bg-soft defined, hex overlays replaced with theme tokens, DevicePairing bg-slate-300 to bg-faint); component/layout consistency (Switch/InputGroup canonical APIs with deprecated aliases, AuthCard ui-form-row spacing, QuickActionBar rounded-xl). Verified: tsc exit 0, vitest 108 files / 541 tests passed, eslint 0 errors (193 pre-existing warnings), build green in 8.06s. Acceptance + evidence contracts (AC-1..AC-7 ALL PASS) validated. Known gap: prettier fmt_cmd not runnable (prettier not a frontend dependency, offline npm cache).
- P1.0 Verification / Cross-System Baseline: CLOSED FOR P1 ENTRY — shared 82 tests, controller-core 205 tests, simulator 35 tests, and backend full suite 393/393 are green; simulator `PublishCommandLifecycle` is exhaustive; backend SQLx PostgreSQL lifecycle is repeatable; frontend Node/npm/npx runtime is now available and `npm run build`, `npx vitest run` (110 files / 546 tests), `npx eslint .` (0 errors), and `npx tsc --noEmit` all pass. Repository-wide Prettier currently reports 294 files because the frontend has no checked-in Prettier configuration/dependency and the existing worktree predates a Prettier baseline; this is diagnosed formatting debt, not a runtime/build/typecheck blocker, and no mass-formatting was performed. Evidence: `docs/evidence/P1.0-VERIFICATION-BASELINE.json`. P1.0 semantic verification gate is closed; formatter normalization is deferred to a dedicated formatting task. No P1.1 feature semantics were introduced by the P1.0 close-out.
- P1.1 Authorization / Ownership Boundary: IMPLEMENTED IN WORKTREE — canonical user/service principal boundary and reusable device/all-device authorization helpers are enforced; all `/api/devices/{device_id}/...` REST surfaces pass through common ownership middleware, legacy `X-User-Id`/`X-Session-Id` headers cannot manufacture user identity, device/admin/config/calibration/season/photo/analytics/health/event/script/recipe/webhook/control capabilities are explicit, fleet user data is filtered through `device_ownership`, and bulk recipe/template operations verify every target before mutation. WebSocket authentication/ownership occurs before stream start. Webhook tokens are device-bound and receive a dedicated `webhook:invoke` capability. Static `ELEVATED_CONTROL_TOKEN` / `X-Elevated-Token` was removed; dangerous control now uses a 60-second backend-issued JWT bound to principal, device, and `dangerous_control`, while normal auth/capability/ownership and `X-User-Confirmed` remain required. Final backend suite: 364/364 PASS; clippy `-D warnings`, fmt check, and `git diff --check` PASS. Frontend runtime verification remains BLOCKED because `node`, `npm`, and `npx` are unavailable. Evidence: `docs/evidence/P1.1-AUTHORIZATION-OWNERSHIP-BOUNDARY.json`.
- P1.2 Durable Command Lifecycle: IMPLEMENTED WITH ENVIRONMENT BLOCK — PostgreSQL `commands` + append-only `command_lifecycle_events` are authoritative; command creation commits durable `REQUESTED` before MQTT with stable `command_id`, scoped idempotency, principal attribution, explicit durable retry-safety classification, bounded retry metadata, and secret-safe payload; lifecycle transitions are row-locked, terminal-immutable, and duplicate-idempotent; ACK/runtime confirmation uses durable authority; recovery emits lifecycle/system events only after commit; retry claims use atomic `FOR UPDATE SKIP LOCKED`; durable history API is live; controller recent command IDs persist in NVS. P1.2 durable lifecycle tests: 15/15 PASS serially; backend check/clippy/fmt/diff-check PASS; live PostgreSQL `retry_safe` schema verified; prior backend baseline 364/364 PASS; shared/controller-core/frontend verification remains green from P1.0/P1.1 baseline. ESP32 application target build remains BLOCKED because ESP-IDF is invoked for unsupported host target `x86_64-unknown-linux-gnu`. No bulk actuator-control route exists in the current API, so AC-12 is explicitly bounded to the existing all-device bulk recipe authorization semantics rather than inventing a new command API.
- P1.3 Safety-Critical Failure Semantics / Operational Truth: IMPLEMENTED WITH VERIFICATION GAPS — cron scheduler now fails closed on missing/error/stale/invalid sensor input and no longer executes from empty fallback payloads; range-stat failures propagate as errors instead of `0`; Flux device/range/resolution inputs are allowlisted, bounded, and escaped; backup/restore enforces device ownership, rejects target mismatch, redacts credential-like fields, and reports publication separately from physical confirmation; actuator interlock/E-stop UI preserves unknown state instead of treating missing pump fields as OFF; bounded safety/Flux/backup outcome metrics added. Verification: backend `cargo check` PASS; targeted scheduler/range-stat/backup tests PASS; shared telemetry tests 6/6 PASS; frontend targeted safety tests 10/10 PASS; frontend build PASS. Full restart/MQTT reconnect runtime verification and complete metric assertions remain open; ESP32 application target remains BLOCKED by existing ESP-IDF host target `x86_64-unknown-linux-gnu`. Evidence: `docs/evidence/P1.3-SAFETY-FAILURE-SEMANTICS.json`.
- P1.4 Operations State Boundary: CLOSED — canonical operational state separates contact, freshness, runtime readiness, and actuator knowledge; 30s freshness threshold is shared; authoritative telemetry preserves observed/received times and rejects delayed actuator/FSM observations; controller reboot invalidates prior actuator/runtime-readiness observations; MQTT subscriptions replay after reconnect; REST/fleet expose operational_state without mapping unknown to offline; frontend preserves unknown/stale; cron action dispatch requires CONTACTED + FRESH; watchdog uses the shared threshold. Verification: shared 67 unit + 22 integration/schema/ingress tests, backend 395/395, watchdog 17/17, frontend 110 files/546 tests + build/tsc + eslint 0 errors; live backend restart, controller restart, and MQTT broker reconnect exercised successfully. Prettier 294-file drift remains diagnosed. Evidence: `docs/evidence/P1.4-OPERATIONS-STATE-BOUNDARY.json`. No P1.5+ semantics introduced.
- DOMAIN-11-PHYSICAL-HIL-E2E: IMPLEMENTED WITH DEPLOYMENT/HIL GAPS — Firebase auth startup race, unavailable config/telemetry semantics, safe durable claim config, physical MQTT/backend/Influx path, and config-sync convergence hardened and evidenced. Local physical sensor -> MQTT -> backend -> Influx/API and physical controller boot/status/command-safety evidence are recorded. Production now runs the merged Domain-11 backend/frontend fix, but existing device_ownership rows can predate safe claim provisioning; backend now backfills only the minimal disabled/manual device_config row at startup so GET /config/unified cannot remain 404 solely because an older claim lacks its base config. Production physical MQTT still does not match the isolated LAN broker used for HIL, so live telemetry remains a separate topology blocker. No actuator activation or emergency-stop action was performed.
 - RELEASE-READINESS-FINAL-002 (2026-09-18): FINAL AUDIT — restarting the dedicated isolated PostgreSQL 16.15 container converted the prior DB setup failure set into a clean authoritative backend result: 430/430 PASS, clippy PASS, fmt PASS. Frontend is 126/126 files and 604/604 tests PASS; SystemLog pagination passes alone and in the full suite. Shared p1_9 schema 7/7 and full shared 71/71 PASS; clippy/fmt PASS; sensor native fixture 14/14 PASS. Domain 10 contract checks pass for shared/backend/frontend/sensor and targeted ConfigurationSync, safety, readiness, backup/restore, and Influx paths. Controller `cargo check --target riscv32imc-esp-espidf` passes after `source ~/export-esp.sh`; controller fixture test execution reaches the configured `espflash` runner but fails `espflash::no_serial` because no physical ESP32-C3 is present. Domain 11 remains partial: existing evidence has isolated physical sensor/controller/backend/Influx observations, but this run cannot verify live physical hardware or production broker/backend topology. Production observability deployment/export is not verified. Release status: INTEGRATION VERIFIED / PRODUCTION CROSS-SYSTEM RELEASE BLOCKED. Canonical final gate evidence: `docs/evidence/RELEASE-READINESS-FINAL-002.json`.
 - RELEASE-READINESS-AUDIT-003 (2026-09-18): CURRENT RE-AUDIT — software cross-system gate remains VERIFIED. Fresh checks: Digital Twin 51 tests + fmt/clippy PASS; controller-core clippy/fmt PASS; backend ConfigurationSync 3/3 PASS; real Backend+PostgreSQL+Mosquitto+Digital Twin E2E 1/1 PASS in 31.46s; frontend 126/126 files and 604/604 tests PASS plus tsc/build PASS. Hardening applied: `/readyz` now requires ConfigurationSync worker health; published config rows are only requeued on MQTT reconnect instead of being republished continuously; route-template metrics remove device-id cardinality; actuator delay covers water pumps; CI governance matches actual ESP32-C3 paths; fallback scripts no longer assume a nonexistent root Cargo workspace. Remaining release blockers are physical HIL, production topology/observability, supported controller target runner, Rust sensor CI/fixture coverage, and legacy production DB upgrade-path verification. Evidence: `docs/evidence/RELEASE-READINESS-AUDIT-003.json`. Earlier P0/P1 lines that say Node/npm/frontend verification is blocked are historical and superseded by P1.0/FINAL-002/AUDIT-003.
 - P2 implementation: SOFTWARE GATE VERIFIED — P2.1 configuration desired-state persistence has an explicit application-service boundary; P2.2 ConfigurationSync remains PostgreSQL-authoritative and bounded, with backup restore transaction tests passing while atomic desired-row reconciliation is not claimed; P2.3 realtime routing/recovery has a deterministic frontend event router; P2.4 route/data boundaries and lazy loading pass the full frontend suite at 127/127 files and 612/612 tests; P2.5 production C++ sensor and controller firmware use the canonical contract and ownership is explicit, while Rust sensor remains experimental/unverified; P2.6 fleet comparison is bounded to 2–4 authorized devices and FleetView adds grid/map, comparison, CSV, and print-to-PDF workflows; P2.7 adds synchronization and command phase-duration metrics. Physical HIL, production topology/observability, and legacy production DB upgrade compatibility remain unverified.
- P3 Frontend Overhaul — P3.4 Operations: COMPLETE FOR SOFTWARE/LOCAL-BROWSER GATE — P3.0 contract, P3.1 shell, P3.2 semantic primitives, and P3.3 Dashboard remain in worktree; Operations Control + Automation surfaces preserve existing React Query/StationContext authorities, command lifecycle distinction, actuator-state semantics, interlocks, and E-stop confirmation. Local development auth uses an explicit loopback-only token/user configuration; authenticated device_001 telemetry was injected through the existing MQTT -> authoritative state -> REST path. Focused P3 tests 25/25 PASS; auth tests 6/6 PASS; frontend build and TypeScript PASS. Backend full suite currently has 2 unrelated config-backup DB test failures. Browser inspected Dashboard and Operations at 1470x690; telemetry refresh observed at ~5s intervals without the prior rate-limit storm. Physical HIL, production topology/observability, and legacy production DB upgrade compatibility remain unverified.
