# Harness Context

Store only material discoveries, decisions, constraints, and unresolved questions that can affect a later phase.

Naming: `phase-<NN>-<slug>-context.md`.

Do not duplicate the design contracts or build log. If a fact already has an authoritative owner, link to it instead.
