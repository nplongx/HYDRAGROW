[package]
name = "hydragrow-supervisor-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
hydragrow-supervisor-query = { path = "../hydragrow-supervisor-query" }
clap = { version = "4.4", features = ["derive", "env"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"
